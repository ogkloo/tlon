{
  "name": "tlon-web-assets",
  "private": true,
  "scripts": {
    "build:agentation": "esbuild client/agentation-entry.jsx --bundle --format=iife --platform=browser --target=es2020 --define:process.env.NODE_ENV='\"development\"' --outfile=static/agentation.bundle.js"
  },
  "dependencies": {
    "agentation": "^3.0.2",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.11"
  }
}
